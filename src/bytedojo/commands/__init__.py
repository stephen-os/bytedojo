"""
Commands package — Click command groups + subcommands for the `dojo` CLI.

The canonical registry of every command lives in
`bytedojo.commands.subcommands`; the top-level Click group is assembled
in `bytedojo.commands.bytedojo`. Nothing currently imports from this
package directly — it's a package marker only.
"""
