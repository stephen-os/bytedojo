{
  "id": 1410,
  "title": "HTML Entity Parser",
  "slug": "html-entity-parser",
  "difficulty": "Medium",
  "description": "HTML entity parser is the parser that takes HTML code as input and replace all the entities of the special characters by the characters itself.\nThe special characters and their entities for HTML are:\nGiven the input text string to the HTML parser, you have to implement the entity parser.\nReturn the text after replacing the entities by the special characters.",
  "examples": [
    {
      "example_num": 1,
      "example_text": "Input: text = \"&amp; is an HTML entity but &ambassador; is not.\"\nOutput: \"& is an HTML entity but &ambassador; is not.\"\nExplanation: The parser will replace the &amp; entity by &",
      "images": []
    },
    {
      "example_num": 2,
      "example_text": "Input: text = \"and I quote: &quot;...&quot;\"\nOutput: \"and I quote: \\\"...\\\"\"",
      "images": []
    }
  ],
  "constraints": ["1 <= text.length <= 105", "The string may contain any possible characters out of all the 256 ASCII characters."],
  "hints": ["Search the string for all the occurrences of the character '&'.", "For every '&' check if it matches an HTML entity by checking the ';' character and if entity found replace it in the answer."],
  "code_snippets": {"cpp": "class Solution {\npublic:\n    string entityParser(string text) {\n        \n    }\n};", "java": "class Solution {\n    public String entityParser(String text) {\n        \n    }\n}", "python3": "class Solution:\n    def entityParser(self, text: str) -> str:\n        ", "javascript": "/**\n * @param {string} text\n * @return {string}\n */\nvar entityParser = function(text) {\n    \n};", "typescript": "function entityParser(text: string): string {\n    \n};", "golang": "func entityParser(text string) string {\n    \n}", "rust": "impl Solution {\n    pub fn entity_parser(text: String) -> String {\n        \n    }\n}"},
  "tags": ["hash-table", "string"],
  "data_structures": [],
  "signature": {
    "params": [
      {
        "name": "text",
        "type": {"base": "STRING"}
      }
    ],
    "returns": {"base": "STRING"}
  }
}
