{
  "problem_id": 2886,
  "input_output": [
    {
      "input": "animals = DataFrame({'name': ['Tatiana', 'Kheli', 'Alex', 'Jonathan', 'Stefan', 'Tommy'], 'species': ['Snake', 'Mongoose', 'Leopard', 'Monkey', 'Bear', 'Panda'], 'age': [98, 17, 6, 45, 100, 26], 'weight': [464, 360, 328, 463, 50, 349]})",
      "output": "DataFrame filtered to weight > 100, sorted by weight descending, selecting only name column"
    },
    {
      "input": "animals = DataFrame with all weights > 100",
      "output": "All names sorted by weight descending"
    },
    {
      "input": "animals = DataFrame with no weights > 100",
      "output": "Empty DataFrame"
    },
    {
      "input": "animals = DataFrame with single animal weight > 100",
      "output": "DataFrame with single name"
    },
    {
      "input": "animals = DataFrame with weights exactly 100",
      "output": "Empty DataFrame (need > 100)"
    },
    {
      "input": "animals = DataFrame with 10 animals, 5 with weight > 100",
      "output": "5 names sorted by weight descending"
    },
    {
      "input": "animals = DataFrame with weights 101, 200, 150",
      "output": "Names sorted: 200 weight first, then 150, then 101"
    },
    {
      "input": "animals = DataFrame with single animal weight <= 100",
      "output": "Empty DataFrame"
    },
    {
      "input": "animals = DataFrame with 100 animals various weights",
      "output": "Filtered and sorted names"
    },
    {
      "input": "animals = DataFrame with weights 500, 400, 300, 200, 100",
      "output": "Names for 500, 400, 300, 200 sorted descending"
    },
    {
      "input": "animals = DataFrame with 3 animals all weight > 100",
      "output": "3 names sorted by weight descending"
    },
    {
      "input": "animals = DataFrame with weights 99, 100, 101",
      "output": "Only name with weight 101"
    },
    {
      "input": "animals = DataFrame with same weight 200 for all",
      "output": "All names (order may vary for same weight)"
    },
    {
      "input": "animals = DataFrame with 2 animals weight > 100",
      "output": "2 names sorted by weight descending"
    },
    {
      "input": "animals = DataFrame with weights 1000, 50, 500, 75, 250",
      "output": "Names for 1000, 500, 250 sorted descending"
    }
  ]
}
